version: "3.9"

services:
  db:
    image: postgres
    volumes:
      - ./db:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: pgdb
      POSTGRES_USER: pguser
      POSTGRES_PASSWORD: pgpass
    restart: always

  transactions_api:
    build: .
    volumes:
      - ./app:/usr/src/app
    tty: true
    command: ./start.sh
    ports:
      - "8000:8000"
    depends_on:
      - db
    restart: always